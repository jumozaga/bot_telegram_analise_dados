from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Bot de Telegram que entrega análise de dados para RH (dados ficticios)recebidos por uma planilha gerada pelos funcionarios com seus feedbacks. [D[D[D',
    author='jumozaga',
    license='',
)
